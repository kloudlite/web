# Access services locally

kloudlite provides a unique feature to access cluster services in local machine using wireguard.
You can create your first device using cli tool `kli` and access your cluster services in local machine.

```bash
kli device create my-device
```